# 서울월드컵경기장 콘서트 데모 작업 기준

> 상태: **현재 가상 배치와 좌표를 후속 데모 작업의 기준으로 채택** (사용자 확인: 2026-09-11).
> 실제 공연 도면·실측 출입구·수용 인원을 확정한 것이 아닙니다. 제품 API·DB 계약과 구분합니다.

## 이 데이터로 진행할 작업

서울월드컵경기장에 미리 준비한 콘서트 배치를 표시하고, 같은 구역·게이트 데이터로
지도 표시와 구역 판정, 신고 위치 시연을 진행합니다. 장소 선정과 기본 배치 작업을 다시 시작하지 않고
아래 파일과 좌표를 사용합니다. 한 행사를 미리 준비해 행사 코드로 진입하는 데모 흐름을 전제로 합니다.
행사 생성·코드 인증·신고 API는 이 지도 시연에 구현되어 있지 않으며 후속 연동 대상입니다.
앱을 종료했다 다시 실행했을 때의 로그인·행사 유지 방식은 보류합니다.

## 소유권과 조회 흐름

행사에 연결된 장소·모델 배치·구역·게이트·데모 지점의 소유자는 **백엔드**입니다.
프론트 담당자는 행사 ID로 지도 API를 호출하고 응답을 렌더링합니다. 이 PR에는 프론트 구현·SDK 설정을 포함하지 않습니다.

1. 프론트 담당자는 데모 행사 ID `69cbb93b-d6cb-5785-a7c8-e606c7279d3d`로 API를 호출합니다.
2. `GET /api/v1/demo/events/{eventId}/map`으로 모델 URL·위치·회전·구역·게이트·데모 지점을 받습니다.
3. 모델과 내려받기 파일은 같은 행사의 `/assets/{assetName}`에서 받습니다.
4. 지형에 따른 높이 계산·카메라·선택·핀 표시는 프론트 어댑터가 처리합니다.

현재는 백엔드가 소유한 **읽기 전용 데모 seed 파일**을 저장소 계층에서 조회합니다. 업무 DB에 저장하는 구현은 아닙니다.
모르는 행사 ID는 지도와 파일 모두 404이며, 허용된 세 파일 외 경로는 제공하지 않습니다.
이 경로는 공개 가상 데모 전용입니다. 인증된 `events/current/map`을 구현한 것으로 간주하지 않습니다.
[데모 API 계약](../api/demo-map.md)을 참고합니다.

## 배치와 데이터 원본

| 항목 | 채택한 값 |
|---|---|
| 장소 | 서울월드컵경기장 |
| 배치 버전 | `seoul-concert-03` |
| 중심 | 경도 `126.89721`, 위도 `37.56817` |
| 회전 | 정북에서 시계방향 `13.5°` |
| 구역 | 플로어 A·B·C·D, 무대·백스테이지, 음향·조명 운영: 6개 |
| 게이트 | 데모 서측 입장·동측 입장·운영 진입: 3개 |
| 모델 | 무대·LED·트러스·계단·스피커·조명 타워·좌석·통로·펜스·운영 부스 |
| 상세도 | 좌석 표현 1,680개, 구성 요소 5,457개, 재질별 메시 15개 |

중심과 회전각은 북쪽이 위인 브이월드 영상의 잔디면 장축을 기준으로 시각 보정했습니다.
좌석 표현 수는 실제 판매 좌석 수나 수용 인원이 아닙니다. 경기장 관중석·지붕 자체의 독립 3D 모델은 포함하지 않습니다.

배치 원본은 [concert-layout.json](../../backend/demo/concert-layout.json)입니다.
기준점에서 동쪽·북쪽 방향의 미터 좌표를 정의하고,
[build-concert.py](../../backend/scripts/build-concert.py)가 같은 기준으로 GLB와 WGS84 좌표를 생성합니다.

| 생성 파일 | 사용처 |
|---|---|
| [seoul-worldcup.json](../../backend/demo/assets/seoul-worldcup.json) | 행사·구역·게이트·모델 위치 데이터 |
| [concert.glb](../../backend/demo/assets/concert.glb) | 콘서트 3D 모델, 1단위 = 1m |
| [concert-zones.geojson](../../backend/demo/assets/concert-zones.geojson) | 닫힌 Polygon 6개와 게이트 Point 3개 |
| [concert-coordinates.csv](../../backend/demo/assets/concert-coordinates.csv) | 구역 꼭짓점·게이트 좌표, UTF-8 BOM |

구역 ID는 구역 키에서 결정적으로 생성합니다. 위치를 조정해도 키를 유지하면 ID는 유지됩니다.
후속 연동은 생성된 `id`로 구역을 참조합니다. 좌표는 WGS84이며 GeoJSON 배열 순서는 **[경도, 위도]**입니다.
변경할 때는 배치 원본을 수정하고 네 파일을 함께 재생성합니다.

## 첫 시연에 사용할 한 점

빨간 `데모 지점`은 **플로어 A 내부**에 표시합니다. 이 값을 후속 지도 표시·신고 시연의 첫 위치로 사용합니다.

```json
{
  "lng": 126.8970733,
  "lat": 37.5683536
}
```

- GeoJSON 좌표: `[126.8970733, 37.5683536]`
- 연결할 구역 ID: `aa9d260d-0125-5c81-a99c-4cd24d0ae015` (플로어 A)
- 마커 입력: 지도 API의 `demoPoint` 필드. 렌더링 구현은 프론트 담당 범위입니다.
- 기대 동작: 해당 좌표를 표시하면 플로어 A 내부에 위치하며, 구역 판정에서도 A로 분류됩니다.

### 높이 적용

브이월드 어댑터에서 이 점의 높이는 **`modelGroundHeight + 0.44`m**입니다.
`modelGroundHeight`는 모델 바닥 범위 15개 지형 표본과 기준 높이에서 계산하는 런타임 값이며,
고정 해발고도가 아닙니다. 데모 API의 `demoPoint.surfaceOffsetMeters`로 오프셋 0.44를 받습니다. `0.44`는 이 데모의 바닥·선택 표면에 맞춘 오프셋입니다.

처음 적용했던 `+2m`는 점이 바닥에서 떠 보여 제거했습니다. 점은 높이 계산 후 표시하고 지형 갱신 시 함께 보정합니다.
다른 렌더러에 연동할 때는 경도·위도를 그대로 사용하되, 이 데모 모델의 바닥 표면에 맞춰 높이를 계산합니다.
GLB의 Y-up과 지도상의 위도/고도를 혼동하지 않습니다. Cesium 어댑터는 `upAxis=Y`, `forwardAxis=X`를 사용합니다.

## 백엔드 실행과 프론트 전달

저장소 루트에서 다음 명령으로 실행합니다. 백엔드의 저장된 데모 데이터 조회에는 브이월드 키가 필요하지 않습니다.

```sh
uv run --directory backend python scripts/build-concert.py  # 배치 수정 시
uv run --directory backend uvicorn app.main:app --host 127.0.0.1 --port 8001
```

지도 조회:

```sh
curl http://127.0.0.1:8001/api/v1/demo/events/69cbb93b-d6cb-5785-a7c8-e606c7279d3d/map
```

프론트 담당자에게 [API 계약](../api/demo-map.md), 행사 ID, `model.uri`와 배치 정보,
구역·게이트·`demoPoint`를 전달합니다. 브이월드 SDK·키 설정·API 프록시 또는 CORS 연동·카메라·표시는
프론트 통합 단계에서 구성합니다. 반환되는 상대 URL은 백엔드 origin 기준입니다.
프론트 구현과 npm 의존성·Vite 설정은 이 PR에서 변경하지 않습니다.

## 브이월드 연동과 API 경계

- SDK: `https://map.vworld.kr/js/webglMapInit.js.do`, version 3.0.
- 브이월드 렌더러 구현은 프론트 담당 범위이며, 아래 내용은 별도 로컬 검증에서 확인한 참고 사항입니다.
- 건물 데이터: `https://cdn.vworld.kr/TDServer/services/map4/TG9ENA.json`.
- 기본 `facility_build` 래퍼는 관찰 당시 타일 1개에서 정체되어 숨겼습니다. SDK에 포함된 Cesium의
  `Cesium3DTileset`으로 같은 제공자 데이터를 직접 로드해 주변 입체 건물을 확인했습니다.
  수신 타일 수는 건물 수가 아니며 경기장 본체의 독립 3D 모델 제공 여부는 미확인입니다.
- PR #48 E05의 구역·게이트 표현을 참고했습니다. 이 문서의 데모 채택은 API 초안 전체 승인이 아닙니다.
  `model`, `color`, `status`, `dataVersion`, 면적 등 시연용 확장 필드는 후속 API·DB 계약에서 별도로 정합니다.

## 검증 결과와 후속 범위

백엔드 통합 검증에서 행사별 응답·모델 URL·세 파일의 원본 일치, 모르는 행사 404,
허용되지 않은 파일 차단을 검사합니다. 제품 인증·DB 계약은 후속 범위입니다.


| 백엔드 검증 | 결과 |
|---|---|
| API 통합 | 행사 데이터·파일 원본 일치, 다른 행사 404, 허용 목록 외 파일 차단 |
| 구조 | GLB 버퍼 범위, 닫힌 Polygon, 구역 장축 13.5° 확인 |
| 테스트 | 132 passed, 3 skipped (별도 PostgreSQL 테스트 DB 미설정) |
| 정적 검사 | Ruff lint/format·Pyrefly 통과 |

재현 명령: `uv run --directory backend pytest`, `uv run --directory backend ruff check .`,
`uv run --directory backend ruff format --check .`, `uv run --directory backend pyrefly check`.

별도로 만든 로컬 화면에서는 실제 브이월드 SDK·영상과 상세 모델의 정렬·좌표·높이를 확인했습니다.
이 시각 검증용 프론트와 Playwright 스크립트는 PR 산출물이 아닙니다.
프론트 담당자의 서비스 화면 통합과 기기별 검증은 후속 작업입니다.

실측 경계·실제 출입구 검증, 경기장 본체 모델링, 로그인·신고 API와 DB 저장,
운영 배포·기기별 성능은 완료 범위에 포함하지 않습니다. 현재 데이터는 후속 데모 작업을 시작할 기준입니다.

## Blender 상세 모델 (2026-09-11)

사용자 요청에 따라 백엔드가 제공하는 GLB 모델을 상세화했습니다. 현재 버전은
`seoul-concert-03-blender-05`이며 23개 메시 노드, 8,691개 구성 요소, 1,680개 좌석 표현을 포함합니다.
좌표·회전·구역·게이트·데모 지점과 기존 지도 API 계약은 유지합니다.
프론트의 지도 렌더링과 브이월드 미리보기는 이 저장소에서 미구현 상태로 유지하며 이번 변경에 포함하지 않습니다.

- [상세 생성기](../../backend/scripts/detail-concert.py): 기본 생성기를 실행한 다음 Blender에서 디테일을 추가합니다.
- [Blender 원본](../../backend/demo/concert.blend): 미터 단위 Z-up 편집 씬입니다. GLB는 Y-up으로 내보냅니다.
- 본무대는 32 × 12 × 2m, 돌출 무대는 폭 5m·길이 36m이며 로컬 북쪽 좌표 2~38m입니다.
- 16cm 상판 아래 기둥·수평재·X자 보강재·받침과 잭을 표현하고 가림막은 제거했습니다.
- LED 모듈, 트러스, 스피커 그릴, 믹싱 콘솔, 부스 표지, 장비 케이스, 케이블 램프, 계단 난간과 바닥 이음매를 추가했습니다.

```sh
blender --background --python backend/scripts/detail-concert.py
```

기본 생성기만 실행하면 상세 모델이 기본 모델로 바뀝니다. 상세 모델 재생성에는 위 명령을 사용합니다.
스크립트 재실행은 `.blend`의 수동 편집을 대체하므로 수동 작업은 다른 파일로 저장합니다.

### 규격 근거와 한계

| 근거 | 확인한 내용 | 모델 반영 |
|---|---|---|
| [서울시설공단 주경기장 이용안내](https://www.sisul.or.kr/open_content/worldcup/guidance/mainst.jsp) | 잔디 117 × 78m, 경기선 105 × 68m | 잔디 기준면 78 × 117m, 보호 바닥 73 × 111m 가운데 정렬 |
| [매일경제 설치 현장 사진](https://www.mk.co.kr/news/society/10803551) | 열린 비계형 무대 하부와 상판 | 상판과 하부 골조를 분리 |
| [Layher Event Systems](https://www.layher.com/en/products/eventsystems), [부품 카탈로그](https://layher-content-prod.s3.eu-central-1.amazonaws.com/s3fs-public/2025-06/EN_Catalogue_Event_2025_1.pdf) | 강관 지름 48.3mm, 0.5m 연결 간격 | 기둥·연결부·베이스 플레이트 표현 |

잔디는 공식 규격이며 무대·객석은 가상 행사 배치입니다. 실제 공연 실측 규격이나 경기장 관중석·지붕, 시공 설계를 재현한 모델은 아닙니다.

검증: Blender 5.2.1 LTS에서 산출물을 생성하고 GLB의 가림막 노드 제거를 확인했습니다.
지도 API 테스트 2개와 백엔드 Ruff·포맷·타입 검사가 통과했습니다.
